apiVersion: v1
kind: Service
metadata:
  name: devops-pipeline-service
  labels:
    app: devops-pipeline-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: devops-pipeline-app
